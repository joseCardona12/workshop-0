import { HomeController } from "./home.controller.js";

export const HomeView = () =>{
    const view = 
    ``;
    const controller = HomeController

    return{
        view,
        controller
    }
}